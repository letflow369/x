(()=>{const root=document.documentElement;const base=root.dataset.siteBase||(/\/artigos\//.test(location.pathname)?'../':'./');const slug=location.pathname.split('/').pop().replace(/\.html$/,'');const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));fetch(`${base}src/data/content-index.json`).then(r=>r.ok?r.json():Promise.reject()).then(data=>{const item=data.items.find(x=>x.slug===slug);if(!item)return;const map=new Map(data.items.map(x=>[x.slug,x]));const section=document.createElement('section');section.className='content-network';section.setAttribute('aria-labelledby','content-network-title');const tags=item.tags.map(t=>`<a class="content-network__tag" href="${base}tags.html?tag=${encodeURIComponent(t)}">#${esc(t)}</a>`).join('');const rel=item.relations.map(r=>map.get(r)).filter(Boolean).slice(0,4).map(x=>`<article class="content-network__card"><p class="content-network__eyebrow">${esc(x.categoryName)}</p><h3><a class="text-link" href="${base}${x.url}">${esc(x.title)}</a></h3><p>${esc(x.summary||'Conteúdo relacionado para aprofundar o tema.')}</p></article>`).join('');section.innerHTML=`<p class="content-network__eyebrow">Rede de conhecimento</p><h2 id="content-network-title">Explore as conexões</h2><p>Esta página possui uma categoria principal e conexões editoriais com outros temas do projeto.</p><div class="content-network__tags" aria-label="Tags relacionadas">${tags}</div>${rel?`<div class="content-network__grid">${rel}</div>`:''}<dl class="content-network__meta"><div><dt>Categoria principal</dt><dd><a class="text-link" href="${base}${item.categoryUrl}">${esc(item.categoryName)}</a></dd></div><div><dt>Tipo de conteúdo</dt><dd>${esc(item.contentType)}</dd></div><div><dt>Metadados de navegação</dt><dd>Atualizados em 10 ago. 2026${item.scientificReview?`<br>Revisão científica declarada na página: ${esc(item.scientificReview)}`:''}</dd></div></dl>`;const main=document.querySelector('main');if(main)main.append(section);}).catch(()=>{});})();
